<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="madmaxstudios.com.chintokaneventmanager">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/chintokan"
        android:label="@string/app_name"
        android:roundIcon="@drawable/chintokan"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Activities.MainActivity"
            android:screenOrientation="portrait"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activities.LoginActivity" />
        <activity
            android:name=".Activities.Homescreen"
            android:label="@string/title_activity_homescreen"
            android:theme="@style/AppTheme" />
        <activity android:name=".Activities.SignupActivity"></activity>
        <activity android:name=".Activities.VerificationActivity"></activity>
    </application>

</manifest>